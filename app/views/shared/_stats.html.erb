<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_user_path(@user) %>" id="following" class="stat">
    <%= @user.following.count %>
    フォロー
  </a>
  <a href="<%= followers_user_path(@user) %>" id="followers" class="stat">
    <%= @user.followers.count %>
    フォロワー
  </a>
</div>